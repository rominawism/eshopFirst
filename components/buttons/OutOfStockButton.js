import React from 'react'
import styles from './OutOfStockButton.module.scss'
const OutOfStockButton = () => {
  return (
    <div className = {`${styles.mainContainer}`}>
        نا موجود
    </div>
  )
}

export default OutOfStockButton